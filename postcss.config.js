const postCssConfig = {
    plugins: [
        require('autoprefixer'), require('tailwindcss')
    ]
}
module.exports = {
    plugins:
        [require('tailwindcss'),
            require('autoprefixer')]
    ,
}
